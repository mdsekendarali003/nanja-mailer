# Invoice Ninja connection (fallback when no config is saved in the app's Settings page)
# Create the API token in Invoice Ninja: Settings → Account Management → Integrations → API Tokens
NINJA_API_URL=https://invoicing.co
NINJA_API_TOKEN=replace-with-your-api-token
